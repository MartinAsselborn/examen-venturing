/*
You must create a mysql database and modify the nodejs/app/src/database.js with you DABASENAME,USER and PASS
RUN THE NEXT COMANDO in the folder nodejs/app:

npm install
npm run dev





RUN THE NEXT COMANDO in the folder /frontreact:

npm install
npm start

*/